apiVersion: v1
kind: Service

metadata:
  name: kronos-redis
  namespace: kronos

  labels:
    app: kronos-redis

spec:
  selector:
    app: kronos-redis

  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP

  type: ClusterIP