apiVersion: batch/v1
kind: CronJob

metadata:
  name: anomaly-detector

spec:
  schedule: "*/30 * * * *"

  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never

          containers:
          - name: anomaly-detector
            image: kronos-anomaly:latest
